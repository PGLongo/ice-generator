# CareCard Environment Variables

# Encryption key for server-side AES-256-GCM encryption (32 bytes = 64 hex chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NUXT_ENCRYPTION_KEY=your-64-character-hex-key-here
